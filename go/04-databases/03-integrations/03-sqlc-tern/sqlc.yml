version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"

    # pode receber um diretório contendo todas as migrações invés de um único arquivo.
    schema: "001_create_table_authors.sql"
    gen:
      go:
        package: "main"
        out: "."
        sql_package: "pgx/v5"
